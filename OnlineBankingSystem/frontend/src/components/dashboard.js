import React, { Component } from "react";

export class dashboard extends Component {
  render() {
    return (
      <div>using two api endpoints to render transaction and its accounts</div>
    );
  }
}

export default dashboard;
