import React, { Component } from "react";

export class filterView extends Component {
  render() {
    return (
      <div>
        using api endpoints to render all transaction and different features.
        Sorting by name, date, search bar
      </div>
    );
  }
}

export default filterView;
